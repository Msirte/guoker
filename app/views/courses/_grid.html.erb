<%# show_filters: :when_filtered %>
<%= grid(@course_grid) do |g|

  g.column name:  '课程编号', attribute: 'id' do |course|
    course.course_code
  end

  g.column name:  '课程名称', attribute: 'name'  do |course|
    course.name
  end

  # g.column  name:  'Description', attribute: 'description' do |course|
  #   course.description
  # end

  # g.column  name:  'Archived', attribute: 'archived' do |course|
  #   course.archived? ? 'Yes' : 'No'
  # end

  # g.column name:  'Due Date', attribute: 'due_date' do |course|
  #   course.due_date
  # end

  # g.column   do |course|
  #   link_to('Edit', edit_course_path(course))
  # end
end -%>